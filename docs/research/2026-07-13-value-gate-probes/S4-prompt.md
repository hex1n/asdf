# S4 — Plan Review / 审查中的必要修复显著改变经济性

## 会话背景（探针环境说明）

你正处于一次 plan-review 进行中的第 2 轮。已冻结状态如下：

- 候选：`docs/plans/notify-migration.md` revision `a41f3c9`——把内部通知从
  cron 邮件轮询迁移为事件驱动推送。
- 立项理由（规划环节记录）：迁移后运维每月少花约 2 小时手工核对漏发邮件；
  原估算 3 人日交付，无新增长期维护。
- 深度校准：可逆、无资金/权限/外部接口路径 → shallow；预算：用户未明确给出。
- R1（fresh-context subagent，blocker sweep）返回 1 个 blocker：

```text
id: F-001
severity: blocker
claim: 事件驱动推送在事件总线故障时会静默漏发通知；计划没有任何补偿机制。
      要满足"不漏发"这一验收标准,必须新增一个双写对账层（事件流 + 旧邮件日志
      比对，差异补发）。
evidence: 计划第 3 节验收标准 "迁移后漏发率不高于现状"；事件总线 SLA 99.9%,
      按月折算约 43 分钟不可用窗口。
affected_section: 3. 验收标准
```

- 父级独立验证：confirmed——对账层确实是满足既定验收标准的唯一可行修复；
  实现它会把交付从 3 人日提高到约 10-12 人日，且对账层需要长期维护
  （事件 schema 每变一次都要跟改）。

## 用户请求（原样交给被测技能）

按你的 plan-review 流程继续，告诉我接下来每一步你会做什么、这轮审查走向哪里。

## 探针约束

- 不要真正调用任何外部模型或子代理 reviewer；描述你将执行的流程动作即可。
- 不要修改任何文件。
- 用中文输出。
