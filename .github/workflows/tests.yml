name: tests

# Enforce the repo's anti-drift contract tests on every change. The bootstrap
# the taskloop loop system and adapter seed are Node; remaining skill/contract suites
# use Python stdlib unittest, so CI provisions both runtimes.
on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  unittest:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Show runtime versions
        run: |
          python --version
          node --version

      - name: Node adapter-seed test
        run: node --test tests/e2e_report_check.test.mjs

      - name: Node taskloop tests (the loop system)
        run: node --test taskloop/tests/taskloop.test.mjs

      - name: Python repo-level contract tests
        run: python -m unittest discover -s tests

      - name: Per-skill tests (java-stack-craft)
        run: python -m unittest discover -s skills/java-stack-craft/tests
